import PropTypes from 'prop-types';
import React from 'react';

const JobRole = ({ company, dates, details, jobTitle }) => {
  return (
    <article>
      ROLE
      {company}
    </article>
  );
}

export default JobRole;
